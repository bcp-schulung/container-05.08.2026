apiVersion: v1
kind: Service
metadata:
  name: joke-gateway
  namespace: joke-platform
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
    nodePort: 30080
